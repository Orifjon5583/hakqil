export function Settings() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Settings</h1>
      <div className="mt-6 rounded border border-line bg-white p-4 text-sm text-slate-600">
        HTTPS, JWT rotation, agent tokens, backup va role permissions sozlamalari keyingi iteratsiyada ulanadi.
      </div>
    </section>
  );
}

